const chapterValidators = require('./chapters.validators')
const audioValidators = require('./audio.validators');
const appConfigValidators = require('./appConfig.validators');

module.exports = {
  chapterValidators,
  audioValidators,
  appConfigValidators,
};
