const ChapterModel = require("./Chapters.model");
const AudioModel = require("./AudioSchema");
const AppConfigModel = require("./AppConfig");

module.exports = {
  ChapterModel,
  AudioModel,
  AppConfigModel,
}
